/**
 * @author Paulo Silva
 * @description Server
 * @returns app with instance of express 
 * @version 1.0.0
 */

/**
 * @requires express server
 */
const express = require('express');

/**
 * @constructor express()
 */
const app = express();

module.exports = app;